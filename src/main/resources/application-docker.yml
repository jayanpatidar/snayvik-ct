spring:
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
  datasource:
    hikari:
      maximum-pool-size: 8

management:
  endpoint:
    health:
      probes:
        enabled: true

app:
  seed:
    enabled: ${APP_SEED_ENABLED:true}
  sync:
    enabled: ${APP_SYNC_ENABLED:false}
